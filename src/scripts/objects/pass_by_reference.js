   var x ={
       "name":"sudhir",
       "marks":"rahul"
   }

   var x =y;
  console.log(y);
  
  y.name="ffff"

  console.log(x);  ///it will cahnge the name beacuse it is referencee type

// In object we are not copy the value it will pointing to reference;
// when the reference object change it will also change

// to copy the value we use Object.assign({}); which create new object also we use spead operatopr to copy the value 





  