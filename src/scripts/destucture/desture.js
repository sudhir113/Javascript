// what is - destucture objects and array injavascript;

maketime({
  name:'suhdir',
  age:23,
  marks:56
});

function maketime({name,age,marks}){
  console.log('this is',name +'have age',age +'got marks',marks);
}